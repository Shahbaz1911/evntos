
import type { Registration } from '@/types';

// This component is no longer in use as the verified guests feature has been removed.
export default function VerifiedGuestListItem({ registration }: { registration: Registration }) {
  return null;
}
